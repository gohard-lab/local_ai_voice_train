[project]
name = "local-ai-voice-train"
version = "0.1.0"
description = "Local AI Voice Trainer Pipeline"
authors = [
    { name = "gohard-lab" }
]
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "pydub>=0.25.1",
    "requests>=2.34.2",
    "streamlit>=1.58.0",
    "streamlit-javascript>=0.1.5",
    "supabase>=2.31.0",
    "torch>=2.12.0",
]

# 💡 스트림릿 서버가 Poetry 빌드 엔진을 돌릴 때 에러를 안 내도록 명시하는 호환성 레이어입니다.
[tool.poetry]
package-mode = false

[tool.uv]
package = false